- email | deferred | Keep real email disabled until internal recipients, verified sender, Resend API key, and explicit preview approval are provided.
- pages | enabled | Public Pages repo is configured with Pages build type `workflow`, `DEPLOY_GITHUB_PAGES=true`, and the live URL `https://osangen.github.io/nk-ai-market-brief-review/` is the Cyril review target.
- auto-refresh | enabled | 4 a.m. Eastern daily retry/watchdog workflow, live freshness gate, fresh-date deploy guard, and live Pages verifier are pushed to source/public repos; public workflow run `25918494116` verified live May 15 output with `mode=auto`, `activeLookbackHours=36`, target hour `4`, new schedules, and send disabled.
- story-diversity | ready | Review output now caps repeated topic clusters at two when enough qualifying items exist; monitor future feed runs for excessive same-topic repeats.
- source-labels | ready | Cards now distinguish actual outlet source from the configured scan label so source labels do not look synthetic.
- copy-polish | ready | Summaries and why-it-matters copy use deterministic variants and no implementation-language filter explanations.
- github-actions | watch | Manual preview workflow passed after push; monitor GitHub action runtime deprecation warnings before Node 20-backed actions are removed from hosted runners.
- agent-observability-release | local-ready | Agent-first logging, retention wiring, and automated verification pass locally; publish only after explicit push/deploy approval, then verify one hosted workflow artifact and live public receipt.
- cyril-review | ready | Share `SHARE_WITH_CYRIL.md` and the GitHub Pages link; ask Cyril to confirm source set, added domains, and page-only vs email delivery.
- v1-source-rings | local-ready | Registry 2.1.0: 57 rows (52-source seed catalog merged, domain-deduped), 40 active at the REQ-1008 cap, 17 shadow; 28 new activations carry live GATE-SOURCE-SMOKE evidence (config/activation-evidence/). Verified locally with a real 40/40 preview run; needs push + one hosted run to verify in CI.
- v1-stack-profile | local-ready | config/norma-stack-profile.json pins MaisonMeta/NormaKamali@519422f0 (read-only snapshot); normaRelevance scoring + NK stack-signal badges live on the review page.
- v1-ai-lane | ready-pending-key | Full key-gated AI lane (packets, budget preflight, REC-BUDGET/REC-PRIVACY receipts, Opus 4.8 via model registry) dry-runs on every build with laneStatus=ready_pending_key. Activation = operator wires ANTHROPIC_API_KEY + NEWSLETTER_AI_LANE_ENABLED=true; private routing stays blocked until a real ZDR receipt.
